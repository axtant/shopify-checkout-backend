name: Build & Deploy to GCP VM

on:
  push:
    branches: [main]
    paths:
      - '../..'

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/checkout-backend

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build & Push Docker image
        working-directory: checkout-backend
        run: |
          docker build -t ${{ env.IMAGE }}:${{ github.sha }} -t ${{ env.IMAGE }}:latest .
          docker push ${{ env.IMAGE }}:${{ github.sha }}
          docker push ${{ env.IMAGE }}:latest

      - name: Deploy to GCP VM via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.GCP_VM_HOST }}
          username: ${{ secrets.GCP_VM_USER }}
          key: ${{ secrets.GCP_VM_SSH_KEY }}
          script: |
            echo ${{ secrets.GHCR_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            docker pull ${{ env.IMAGE }}:latest

            docker stop checkout-backend || true
            docker rm checkout-backend || true

            docker run -d \
              --name checkout-backend \
              --restart unless-stopped \
              -p 8080:8080 \
              -v /home/cs2/checkout-config:/app/config \
              -e SPRING_CONFIG_ADDITIONAL_LOCATION=file:/app/config/ \
              ${{ env.IMAGE }}:latest
